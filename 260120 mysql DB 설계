- mysql 접속
1. mysql -u [계정] -p 후 비밀번호 작성
2. show databases;
3. show tables from [databases 이름];
4. use [databases 이름];


- 회원테이블 member
   1. 회원번호 no int not null auto_increment unique
    : unique 중복 허용 X, null값 허용 
   2. 아이디 id varchar(50) not null primary key
     : primary key 중복 허용 X, null값 허용 X 
   3. 비밀번호 password varchar(50) not null
   4. 이름 name varchar(50) not null
   5. 전화번호 phone varchar(20) not null
   6. 등록일 createdData  DATETIME DEFAULT CURRENT_TIMESTAMP


- Table 생성
   create table member(
   no int not null auto_increment unique, 
   id varchar(50) not null primary key,
   password varchar(50) not null, 
   name varchar(50) not null,
   phone varchar(50) not null,
   createdDate DATETIME DEFAULT CURRENT_TIMESTAMP);

- 데이터값 입력
insert into member(no, id, password, name, phone, createdDate) 
values(null, 'hong', '1234', '홍길동', '010-1111-1111', now());
: no : 테이블 생성시 auto_increment 입력하여 null값 작성
: createdDate : 데이터값 now() 입력하여 현재 시간이 작성

또는
insert into member values (null, 'hong', '1234', '홍길동', '010-1111-1111', now());

- 제약조건
